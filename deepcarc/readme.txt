1, The main.py is the only file you need to edit for repeating the results for the DeepCarc model (mol2vec and supervised base classifiers selection method). More details are included in the main.py
2, All the *.py scripts should be located in the same directory for running the main.py
3, validation_probabilities_supervised.csv and test_probabilities_supervised.csv are the model-level representation combined from the five base classifiers. These are the reference files. You may compare those with the one you have generated.